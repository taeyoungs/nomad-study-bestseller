import { Dancing_Script, Raleway } from 'next/font/google';

export const raleway = Raleway({
  display: 'swap',
  subsets: ['latin'],
  variable: '--font-raleway',
});

export const dacingScript = Dancing_Script({
  display: 'swap',
  subsets: ['latin'],
  variable: '--font-dancing-script',
});
