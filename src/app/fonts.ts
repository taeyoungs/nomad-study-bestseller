import { Dancing_Script, Raleway } from 'next/font/google';

export const raleway = Raleway({ display: 'swap', subsets: ['latin'] });
export const dacingScript = Dancing_Script({ display: 'swap', subsets: ['latin'] });
